library(dplyr)
library(tidyverse)
head(mtcars)
dim(mtcars)
filter(mtcars, mpg > 20)
mtcars$make_model <- rownames(mtcars)
head(mtcars)
filter(mtcars, qsec < 17)
filter(mtcars, mpg > 20,
qsec < 17,
gear >= 5)
mtcars <- mtcars[, 1:11]
arrange(mtcars)
arrange(mtcars, qsec)
arrange(mtcars, desc(qsec))
arrange(mtcars, desc(cyl),
desc(wt))
df <- mtcars
df %>%
filter(mpg > 20)
df %>%
filter(mpg > 20,
gear >= 4) %>%
arrange(qsec)
df <- mtcars
df$new_wt <- 1000 * df$wt
head(df)
df$new_col <- df$cyl +
df$wt +
df$am +
df$gear
head(df)
df <- df %>%
mutate(new_col = cyl + wt + am + gear)
head(df)
df <- df %>%
mutate(new_col = 1,
new_wt = 1000 * wt)
head(df)
df %>%
mutate(decile = ntile(qsec, 10)) %>%
head()
df <- mtcars
head(df)
df %>%
mutate(decile = ntile(qsec, 10)) %>%
filter(decile <= 3)
df %>%
mutate(decile = ntile(qsec, 10),
over_20_mpg = ifelse(mpg > 20, 1, 0)) %>%
filter(decile <= 3,
over_20_mpg == 1)
df %>%
mutate(decile = ntile(qsec, 10),
over_20_mpg = ifelse(mpg > 20, 1, 0)) %>%
filter(decile <= 3,
over_20_mpg == 1)%>%
arrange(wt)
df %>%
mutate(decile = ntile(qsec, 10),
over_20_mpg = ifelse(mpg > 20, 1, 0)) %>%
filter(decile <= 3,
over_20_mpg == 1)%>%
arrange(wt)%>%
select(mpg, wt, qsec)
# Load required libraries
# dplyr is used for data manipulation
# tidyverse contains dplyr, ggplot2, tidyr and other packages
library(dplyr)
library(tidyverse)
head(mtcars)
dim(mtcars)
filter(mtcars, mpg > 20)
mtcars$make_model <- rownames(mtcars)
# Display first 6 rows again to verify new column
head(mtcars)
# Filter cars with quarter-mile time less than 17 seconds
# Lower qsec generally means faster acceleration
filter(mtcars, qsec < 17)
---
title: "dplyr Operations on mtcars Dataset"
# Introduction
This R Markdown document demonstrates the use of major functions from the `dplyr` and `tidyverse` packages for:
- Filtering rows
The built-in `mtcars` dataset is used throughout the examples.
---
# Load Libraries
```r
# Filter Operations
```r
